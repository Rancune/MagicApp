using System.Collections;
using System.Collections.Generic;
using UnityEngine;




public class ScoreTracking : MonoBehaviour
{
  static public string[,] scoreArray;
 // List<Word> words = new List<Word>();


    // Start is called before the first frame update
    void Start()
    {
        //Init du tableau avec les noms des participants
       // void DisplayArray(string[] initArray) => Console.WriteLine(string.Join(" ", arr));
    

    }

    // Update is called once per frame
    void Update()
    {
        
    }

    static void InitScore(string[] initArray)
    {
       /*for (int i = 0; i <= initArray.GetLength; i++) 
       {
            scoreArray [0,i] = initArray[i];
            scoreArray [1,i] = "0";
       }*/

      // words.Add(new Word(word));
       //words.ToArray();
    }
}


